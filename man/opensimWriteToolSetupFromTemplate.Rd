% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opensim-setup.R
\name{opensimWriteToolSetupFromTemplate}
\alias{opensimWriteToolSetupFromTemplate}
\title{Write OpenSim Tool Setup XML from Template}
\usage{
opensimWriteToolSetupFromTemplate(
  template_file,
  output_file,
  fields,
  strict = TRUE
)
}
\arguments{
\item{template_file}{Path to template XML.}

\item{output_file}{Path to output XML.}

\item{fields}{Named list of replacement values keyed by XML tag name.}

\item{strict}{If \code{TRUE}, error when a tag in \code{fields} is missing in template.}
}
\value{
A named list with \code{output_file}, \code{applied_tags}, and \code{missing_tags}.
}
\description{
Replaces tag values in an existing OpenSim setup XML template.
This function is tool-agnostic and can be used for IK/ID/SO/RRA/CMC templates.
}
\examples{
# Create a minimal template in a temp file
tpl <- tempfile(fileext = ".xml")
writeLines(c(
  "<OpenSimDocument>",
  "  <model_file>Unassigned</model_file>",
  "  <time_range>0 1</time_range>",
  "</OpenSimDocument>"
), tpl)

out <- tempfile(fileext = ".xml")
result <- opensimWriteToolSetupFromTemplate(
  template_file = tpl,
  output_file = out,
  fields = list(model_file = "my_model.osim", time_range = "0.5 2.0")
)
result$applied_tags
readLines(result$output_file)
}
