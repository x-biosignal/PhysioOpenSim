% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opensim-cli.R
\name{opensimRunTool}
\alias{opensimRunTool}
\title{Run an OpenSim Tool Setup XML}
\usage{
opensimRunTool(
  setup_file,
  workdir = NULL,
  cli = NULL,
  extra_args = character(),
  timeout_sec = 0L,
  fail_on_error = TRUE,
  execution = c("auto", "native", "cli"),
  tool_type = c("tool", "ik", "id", "so", "analyze", "cmc", "rra")
)
}
\arguments{
\item{setup_file}{Path to OpenSim tool setup XML.}

\item{workdir}{Optional working directory.}

\item{cli}{Optional command or absolute path (CLI mode only).}

\item{extra_args}{Optional extra CLI args appended after setup file.}

\item{timeout_sec}{Timeout in seconds for CLI execution (0 disables timeout).}

\item{fail_on_error}{If \code{TRUE}, stop on non-zero exit status.}

\item{execution}{Execution backend: \code{"auto"} (default), \code{"native"}, \code{"cli"}.}

\item{tool_type}{Tool flavor token: \code{"tool"} (generic), \code{"ik"}, \code{"id"}, or
\code{"so"}, \code{"analyze"}, \code{"cmc"}, \code{"rra"}.}
}
\value{
A named list with command metadata and logs.
}
\description{
Executes a setup XML through native OpenSim bindings (when available) or
through OpenSim CLI.
}
\examples{
\dontrun{
result <- opensimRunTool("ik_setup.xml")
result$status
result$stdout

# Force CLI backend with extra arguments
opensimRunTool("ik_setup.xml", execution = "cli",
               extra_args = "--visualize")
}
}
